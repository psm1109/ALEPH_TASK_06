# 저장소 작업 규칙

## 1. 작업 시작 전 확인

이 저장소를 수정하기 전에 반드시 `PROJECT_CONTEXT.md`를 읽습니다.

현재 체크아웃을 기준으로 다음 항목이 문서와 일치하는지 확인합니다.

- 현재 브랜치와 기준 커밋
- 작업 트리의 기존 변경 사항
- 마지막으로 통과한 검사와 실행 명령
- 실제 Supabase 및 배포 환경의 확인 여부
- 아직 완료되지 않은 작업과 다음 순서

확인 비용이 크지 않은 정보는 기존 기록만 믿지 말고 다시 확인합니다.

## 2. 작업 종료와 인계 문서

하나의 작업 단위가 끝날 때마다 `PROJECT_CONTEXT.md`를 최신 상태로 갱신합니다.

반드시 기록할 내용은 다음과 같습니다.

1. Asia/Seoul 기준 마지막 수정 날짜
2. 현재 브랜치와 기준 커밋
3. 이번에 완료한 작업
4. 수정한 주요 파일 경로
5. 실행한 검사 명령과 실제 결과
6. 아직 확인하지 못했거나 해결하지 못한 항목
7. 다음 작업자가 바로 실행할 구체적인 다음 단계

다음 원칙도 함께 지킵니다.

- 코드·정적 검사 결과와 실제 Supabase·배포 브라우저 확인 결과를 명확히 구분합니다.
- 실행하지 않은 SQL이나 확인하지 않은 운영 흐름을 완료했다고 기록하지 않습니다.
- 다른 PC에서도 대화 기록 없이 이어갈 수 있도록 필요한 파일 경로와 명령을 남깁니다.
- 오래된 진행 기록을 계속 덧붙이지 않고 현재 상태와 맞지 않는 내용을 최신 정보로 교체합니다.

## 3. 보안 및 검증 원칙

- 비밀번호 원문, access token, refresh token, secret/service-role key, JWT 비밀키, 쿠키를 코드·문서·로그에 기록하지 않습니다.
- 인증 요청과 응답을 증거로 남길 때 민감한 값은 반드시 `[가림]`으로 바꿉니다.
- 공개 가능한 Publishable key와 서버 비밀키를 구분합니다.
- 기존 로직에 영향을 줄 수 있는 변경은 예상되는 사이드 이펙트를 먼저 검토합니다.
- 변경 범위에 맞는 테스트와 정적 검사를 실행한 뒤 결과를 보고합니다.
- 실제로 확인하지 못한 항목은 추측하지 않고 미확인 또는 차단 상태로 기록합니다.

## 4. 커밋 메시지 규칙

모든 커밋 메시지는 아래 Prefix와 형식을 준수합니다.

### Prefix

- `feat`: 사용자에게 새로운 기능 추가
- `fix`: 잘못된 동작이나 오류 수정
- `refactor`: 동작 변경 없이 코드 구조 개선
- `style`: 디자인, CSS, 문구 등 화면 표현 변경
- `test`: 테스트 추가 또는 수정
- `docs`: README 등 문서 변경
- `chore`: 설정, 빌드, 배포 등 부수 작업

### 작성 형식

```text
<prefix>: <대표 변경 내용을 한 문장으로 요약>

- [영역] 상세 변경 내용 1
- [영역] 상세 변경 내용 2
```

세부 항목의 영역 표시는 변경 성격에 맞게 `[Feat]`, `[Fix]`, `[DB]`, `[Docs]`, `[Test]`, `[Security]` 등을 사용합니다.

예시:

```text
feat: 전체 자료 내보내기와 공개 경고 추가

- [Feat] 첫 화면에 로그인 미적용 공개 범위 경고 표시
- [Feat] 전체 PDS 자료를 JSON 파일 하나로 내보내기
- [Feat] 최신 Supabase 자료를 확인한 뒤 내보내도록 구성
- [Docs] 데이터 표·항목·관계·날짜 규칙 계약서 추가
- [Fix] 날짜 표시와 날짜별 집계를 서울 시간으로 통일
- [Security] XSS 이스케이프와 비밀키 누출 여부 검증
```

## 5. AI 답변 원칙

- 답변은 결과부터 간결하고 명확하게 작성합니다.
- 코드는 전체 파일보다 수정된 부분과 판단에 필요한 부분을 중심으로 보여줍니다.
- 코드 수정 전 기존 로직과 데이터에 미칠 사이드 이펙트를 먼저 검토합니다.
- 복잡하거나 의도가 드러나지 않는 로직에는 동작 설명이 아닌 설계 의도를 주석으로 남깁니다.
- 작업 완료 보고에는 수정 내용, 검사 결과, 커밋 해시, 남은 문제를 포함합니다.
- 검증하지 않은 내용을 완료한 것처럼 표현하지 않습니다.
