# 저장소 작업 인계 규칙

이 저장소를 수정하기 전에 `PROJECT_CONTEXT.md`를 읽습니다. 현재 체크아웃을 기준으로 브랜치·커밋·검사 결과·운영 서비스 상태가 여전히 맞는지, 확인 비용이 크지 않다면 다시 확인합니다.

하나의 작업 단위가 끝날 때마다 다음 규칙을 지킵니다.

1. `PROJECT_CONTEXT.md`에 Asia/Seoul 기준 수정 날짜, 현재 브랜치, 기준 커밋, 완료한 작업, 실행한 확인 명령과 결과, 남은 문제, 다음 작업을 기록합니다.
2. 코드·정적 검사 결과와 실제 Supabase·배포 브라우저 확인 결과를 구분합니다. 실행하지 않은 SQL이나 확인하지 않은 운영 흐름을 완료했다고 적지 않습니다.
3. 다른 PC에서도 대화 기록 없이 이어갈 수 있도록 관련 파일 경로와 실행 명령을 남깁니다.
4. 비밀번호 원문, access token, refresh token, secret/service-role key, JWT 비밀키, 가리지 않은 인증 요청 본문을 기록하지 않습니다. 증거에는 `[가림]`을 사용합니다.
5. `PROJECT_CONTEXT.md`에는 현재 상태를 중심으로 기록합니다. 오래된 진행 일지를 계속 덧붙이지 말고 더 이상 맞지 않는 상태를 최신 내용으로 바꿉니다.

